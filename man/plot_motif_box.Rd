% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot.R
\name{plot_motif_box}
\alias{plot_motif_box}
\title{Boxplot of normalized motif counts (internal helper)}
\usage{
plot_motif_box(
  norm_counts,
  motif_key,
  layer = NULL,
  sample_df = NULL,
  group_var = NULL,
  offset_mode = NULL
)
}
\arguments{
\item{norm_counts}{List of normalized counts (e.g., \code{cellgraph$norm_counts} from \code{\link[=count_motifs_graphs]{count_motifs_graphs()}}); when multiple offset modes are present, select one via \code{offset_mode}.}

\item{motif_key}{Character motif identifier with prefix (e.g., \code{"E_A_B"}).}

\item{layer}{Which layer to plot; when \code{NULL}, inferred from the motif prefix (\code{N_}, \code{E_}, \code{T_}, \code{W_}, or \code{TW_}).}

\item{sample_df}{Optional data frame of sample metadata; rownames must match the motif columns.}

\item{group_var}{Optional column name in \code{sample_df} to use for grouping/coloring the boxplot.}
}
\value{
A \code{ggplot} object.
}
\description{
Visualize normalized motif counts (e.g., from \code{\link[=get_norm_counts]{get_norm_counts()}}) across samples for a given motif.
}
\keyword{internal}

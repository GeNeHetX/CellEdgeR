% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot.R
\name{plot_sample_graph}
\alias{plot_sample_graph}
\title{Plot a sample graph with highlighted motifs}
\usage{
plot_sample_graph(
  graph_obj,
  sample_name,
  max_edge_len = Inf,
  highlight_labels = NULL,
  motif_key = NULL,
  motif_layer = NULL,
  cells_by_sample = NULL,
  motif_node_size = 3,
  dim_node_nonmotif = 0.6,
  alpha_node_nonmotif = 0.4,
  alpha_edge_nonmotif = 0.3
)
}
\arguments{
\item{graph_obj}{Output of \code{\link[=build_cell_graphs]{build_cell_graphs()}} with stored coordinates.}

\item{sample_name}{Sample name to plot.}

\item{max_edge_len}{Optional numeric threshold to prune long edges for display; set \code{Inf} to keep all.}

\item{highlight_labels}{Optional character vector of labels to emphasize.}

\item{motif_key}{Optional motif identifier (e.g., \code{"E_A_B"}); when provided, the labels in the key
are highlighted and edges connecting those labels are accentuated.}

\item{motif_layer}{Layer for the motif key; when \code{NULL}, inferred from the motif prefix.}

\item{cells_by_sample}{Optional named list of raw sample data frames; used only when \code{graph_obj}
lacks stored coordinates (backward compatibility with older objects).}

\item{motif_node_size}{Size for nodes participating in the highlighted motif.}

\item{dim_node_nonmotif}{Factor to shrink nodes that are not in the highlighted motif.}

\item{alpha_node_nonmotif}{Alpha for nodes that are not in the highlighted motif.}

\item{alpha_edge_nonmotif}{Alpha for edges that are not in the highlighted motif.}
}
\value{
A \code{ggplot} object.
}
\description{
Draw the Delaunay edges and cell coordinates for a single sample, optionally highlighting
a motif by labels (and edges connecting those labels).
}

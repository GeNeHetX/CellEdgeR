% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot.R
\name{plot_motif_volcano}
\alias{plot_motif_volcano}
\title{Volcano plot for motif differential results}
\usage{
plot_motif_volcano(
  x,
  result_type = c("edges", "triplets_cooccurrence", "triplets_topology"),
  coef = NULL,
  model = c("full", "null"),
  fdr_method = "BH",
  fdr_cutoff = 0.05,
  logFC_cutoff = 0,
  label_mode = c("none", "manual", "top_n", "top_bottom_n"),
  label_motifs = NULL,
  label_n = 10,
  label_top_by = c("PValue", "FDR", "abs_logFC"),
  point_size = 1.8,
  point_alpha = 0.8,
  title = NULL,
  subtitle = NULL
)
}
\arguments{
\item{x}{A results data frame (e.g., output of \code{\link[=top_edges]{top_edges()}} or \code{\link[=top_triplets]{top_triplets()}}),
or a \code{cellEdgeR_obj} containing \code{motif_edger()} results.}

\item{result_type}{Which results to fetch when \code{x} is a \code{cellEdgeR_obj}:
\code{"edges"}, \code{"triplets_cooccurrence"}, or \code{"triplets_topology"}.}

\item{coef}{Coefficient name or index passed to \code{\link[=top_edges]{top_edges()}} / \code{\link[=top_triplets]{top_triplets()}}
when \code{x} is a \code{cellEdgeR_obj}.}

\item{model}{Model (\code{"full"} or \code{"null"}) used for \code{"edges"} and \code{"triplets_cooccurrence"}
when \code{x} is a \code{cellEdgeR_obj}.}

\item{fdr_method}{Multiple testing correction method passed to \code{p.adjust} when needed.}

\item{fdr_cutoff}{FDR threshold used for point coloring.}

\item{logFC_cutoff}{Absolute logFC threshold used for point coloring.}

\item{label_mode}{Label selection mode: \code{"none"}, \code{"manual"}, \code{"top_n"}, \code{"top_bottom_n"}.}

\item{label_motifs}{Motif keys to label when \code{label_mode = "manual"}.}

\item{label_n}{Number of motifs to label for \code{"top_n"} and \code{"top_bottom_n"}.}

\item{label_top_by}{Ranking criterion for \code{"top_n"} labels: \code{"PValue"}, \code{"FDR"}, \code{"abs_logFC"}.}

\item{point_size}{Point size in the volcano.}

\item{point_alpha}{Point alpha in the volcano.}

\item{title}{Optional plot title.}

\item{subtitle}{Optional plot subtitle.}
}
\value{
A \code{ggplot} object.
}
\description{
Plot differential motif results (from \code{\link[=top_edges]{top_edges()}} or \code{\link[=top_triplets]{top_triplets()}}) in a
standard volcano layout, with flexible motif labeling options.
}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot.R
\name{plot_motif_volcano_triptych}
\alias{plot_motif_volcano_triptych}
\title{Triptych volcano with ordered motif side panels}
\usage{
plot_motif_volcano_triptych(
  x,
  result_type = c("edges", "motifs2"),
  coef = NULL,
  model = c("full", "null"),
  fdr_method = "BH",
  n_side = 5,
  left_motifs = NULL,
  right_motifs = NULL,
  label_side_motifs = TRUE,
  side_text_mode = c("numbered", "verbose"),
  side_label_truncate = 3,
  label_mode = c("none", "manual", "top_n", "top_bottom_n"),
  label_motifs = NULL,
  label_n = 6,
  label_top_by = c("PValue", "FDR", "abs_logFC"),
  fdr_cutoff = 0.05,
  logFC_cutoff = 0,
  point_size = 1.8,
  point_alpha = 0.8,
  panel_widths = c(1.35, 2.4, 1.35),
  title = NULL,
  subtitle = NULL
)
}
\arguments{
\item{x}{A results data frame (e.g., output of \code{\link[=top_edges]{top_edges()}} or \code{\link[=top_motifs2]{top_motifs2()}}),
or a \code{cellEdgeR_obj} containing \code{motif_edger()} results.}

\item{result_type}{Which results to fetch when \code{x} is a \code{cellEdgeR_obj}:
\code{"edges"} or \code{"motifs2"}.}

\item{coef}{Coefficient name or index passed to \code{\link[=top_edges]{top_edges()}} / \code{\link[=top_motifs2]{top_motifs2()}}
when \code{x} is a \code{cellEdgeR_obj}.}

\item{model}{Model (\code{"full"} or \code{"null"}) used when \code{x} is a \code{cellEdgeR_obj}.}

\item{fdr_method}{Multiple testing correction method passed to \code{p.adjust} when needed.}

\item{n_side}{Number of automatically selected motifs on each side.}

\item{left_motifs}{Optional motif keys to force on the depleted (left) panel.}

\item{right_motifs}{Optional motif keys to force on the enriched (right) panel.}

\item{label_side_motifs}{Logical; if \code{TRUE}, motifs shown on side panels are labeled on the volcano.}

\item{side_text_mode}{Side-panel annotation mode. \code{"numbered"} (default) uses compact
numeric IDs shared with volcano labels; \code{"verbose"} uses the previous motif/logFC/FDR text.}

\item{side_label_truncate}{Number of characters to keep for cell labels inside side-panel motifs.
Set to \code{NA} for no truncation.}

\item{label_mode}{Additional volcano label mode: \code{"none"}, \code{"manual"}, \code{"top_n"}, \code{"top_bottom_n"}.}

\item{label_motifs}{Motif keys to label when \code{label_mode = "manual"}.}

\item{label_n}{Number of motifs to label for \code{"top_n"} and \code{"top_bottom_n"}.}

\item{label_top_by}{Ranking criterion for \code{"top_n"} labels: \code{"PValue"}, \code{"FDR"}, \code{"abs_logFC"}.}

\item{fdr_cutoff}{FDR threshold used for volcano point coloring.}

\item{logFC_cutoff}{Absolute logFC threshold used for volcano point coloring.}

\item{point_size}{Point size in the volcano.}

\item{point_alpha}{Point alpha in the volcano.}

\item{panel_widths}{Relative widths for left/center/right panels.}

\item{title}{Optional volcano title.}

\item{subtitle}{Optional volcano subtitle.}
}
\value{
If package \code{patchwork} is installed, returns a combined patchwork plot.
Otherwise draws the 3 panels and returns a list with components:
\code{depleted_panel}, \code{volcano}, \code{enriched_panel}.
}
\description{
Create a 3-panel figure with the most depleted motifs on the left, the volcano in the middle,
and the most enriched motifs on the right. Side motifs are displayed in fixed top-to-bottom order.
}
